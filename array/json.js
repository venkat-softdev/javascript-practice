// json in javascript 

let person={
    name:"venkat",
    age:20,
    city:"vadalur"
};

console.log(person,typeof person);

let jsonfile=JSON.stringify(person);
console.log(person,typeof person);

